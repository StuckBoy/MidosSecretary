package interfaces;

/**
 * Classes implementing this interface will support the printing of their
 * internal information in a way that is easily understood and well-structured.
 */
public interface Printable {
    /**
     * Prints relevant information in a way that is easily understandable in a
     * console-based system.
     */
    void print();
}
